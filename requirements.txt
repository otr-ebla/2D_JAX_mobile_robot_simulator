# Core simulator dependencies excluding JAX itself.
# Install the accelerator-specific JAX build that matches your hardware as
# documented in README.md before installing this file.
numpy>=1.24
matplotlib>=3.7
# Core dependencies for the 2D JAX mobile robot simulator
# Install the accelerator-specific jaxlib build that matches your hardware.
jax>=0.4.31
jaxlib>=0.4.31
